<h1><%= @project.name %></h1>
<ul>
  <% @entries.each do |entry| %>
  <li>
    <%= entry.hours %> h
    <%= entry.minutes %> m -
    <%= entry.date %>
  </li>
  <% end %>
</ul>
<%= link_to('Go Back', projects_path) %>
<br>
<%= link_to('New Entry', new_project_entry_path(@project)) %>
